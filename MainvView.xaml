<UserControl x:Class="WpfSimpleCal.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfSimpleCal"        
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=local:MainViewModel,IsDesignTimeCreatable=True}"        
        Height="600" Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="190"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!--위쪽 열 부분에 대한 디자인 -->
        <Border Grid.Row="0" BorderThickness="2" Background="Aquamarine" Margin="10,10,20,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                    <Image Source="{Binding MainIcon}" Width="150" Height="150"/>
                </StackPanel>
                <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="메모리 리스트 확인" FontSize="25" FontWeight="Bold" FontFamily="Hancom MalangMalang Bold" Height="31"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0,20,0" Width="490">
                    <Button Width="100" Height="100" Command="{Binding ReloadMemoryCommand}" BorderThickness="0" BorderBrush="Transparent" Background="Transparent" Margin="20">
                        <Image Source="{Binding Searchbtn}" Width="80" Height="80"/>
                    </Button>
                    <Button Width="100" Height="100" Command="{Binding ClearListboxCommand}" BorderThickness="0" BorderBrush="Transparent" Background="Transparent" Margin="30">
                        <Image Source="{Binding ClearList}" Width="80" Height="80"/>
                    </Button>
                    <Button Width="100" Height="100" Command="{Binding RemoveSelectedCommand}" BorderThickness="0" BorderBrush="Transparent" Background="Transparent" Margin="30">
                        <Image Source="{Binding Closebtn}" Width="80" Height="80"/>
                    </Button>
                </StackPanel>
            </StackPanel>            
        </Border>
        <Border Grid.Row="1" BorderThickness="2" Background="LightCyan" Margin="10,10,20,10">
            <DataGrid ItemsSource="{Binding ProcessQueue}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" Margin="10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ProgramID" Binding="{Binding ProcessId}" FontWeight="Bold" Width="100"/>
                    <DataGridTextColumn Header="ProgramName" Binding="{Binding ProgramName, StringFormat=' ({0})'}" Width="450"/>
                    <DataGridTextColumn Header="ViewTime" Binding="{Binding AddedTime, StringFormat='yyyy-MM-dd HH:mm:ss'}" Foreground="Gray" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
